
Interpretator состоит из AsyncTcpServ, AsyncUdpServ, logOutput, Server, Interpretator, trik-sharp
trik-sharp - см. выше
AsyncTcpServ - асинхронный TCP сервер, по прин€тию сообщений генерирует событи€,
так же генерирует событи€ когда соединение установленно/сброшенно
AsyncUdpServ - асинхронный UDP сервер, принимает запрос на IP и mac, и соответсвенно
в ответ отсылает mac и IP. 
Server - контролирует работу TCP и UDP, выключает UDP как только установленно соединение
по TCP, включает UDP если соединение сброшено. “ак же отправл€ет полученные событи€ 
о прин€тии сообщений в Interpretator.
Interpretator - интерпретирует полученные сообщени€.


јлгоритм работы в целом следующий:
ѕоднимаетс€ UDP и TCP сервер, по UDP ждЄт запроса на отправку mac адресса и IP текущего
контроллера TRIK, как только получен запрос, отсылает mac и IP. 
 ак только с ним устанавливаетс€ соеднинение по TCP, UDP сервер выключаетс€
≈сли произошЄл разрыв соединени€ по TCP, то возобновл€етс€ работа UDP.
UDP и TCP асинхронные(их контролирует Server) 


TCP по приЄму сообщени€ генерирует событи€, которые обрабатывает Interpretator.
 оманды ожидаютс€ в следующем формате:
массив байтов, первые два байта - скорость на моторы, от -100 до 100, 
третий байт - когда 1, тогда совершаетс€ пинок(на основе сервопривода)
четвЄртый байт - включить на светодиоде необходимый свет:
0 - оставить как было, 1 - выключить, 2 - зелЄный, 3 - оранжевый, 4 - красный
